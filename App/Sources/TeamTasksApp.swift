import SwiftUI
import TeamTasksCore

// Phase 0 placeholder — replaced by the real app shell (Environment, RootView, tabs) in phase 2C.
@main
struct TeamTasksApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Équipe")
                .font(.largeTitle.bold())
        }
    }
}
