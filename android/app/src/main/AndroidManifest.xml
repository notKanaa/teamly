<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Due-date reminders and « Nouvelle tâche » notifications (runtime permission on Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Pending reminders are alarms, which a reboot clears: they are set again after boot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Nothing is backed up or transferred to another device: the Auth session (like the iOS Keychain item « this
        device only ») and the notification state belong to this installation.
    -->
    <application
        android:name=".EquipeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Equipe"
        tools:targetApi="33">

        <!--
            Single activity. Portrait only, like the iOS app (Android 16+ ignores it on large screens, where the layouts
            adapt anyway). Never recreated by a configuration change (dark mode, font scale, keyboard, locale…): Compose
            handles them, and the remembered screen models survive. singleTop: `equipe://` links and notification taps
            reach onNewIntent.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|fontScale|keyboard|keyboardHidden|navigation|touchscreen|uiMode|locale|layoutDirection|colorMode|mcc|mnc"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Equipe.Starting"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- equipe://task/<groupId>/<taskId> (ntfy « click » field), equipe://group/<groupId>, equipe://mytasks. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="equipe" />
            </intent-filter>
        </activity>

        <!-- Fires a due-date reminder (inexact alarm set by AndroidNotificationScheduler). -->
        <receiver
            android:name=".platform.ReminderAlarmReceiver"
            android:exported="false" />

        <!-- Sets the pending reminders again after a reboot (the system clears alarms). -->
        <receiver
            android:name=".platform.NotificationRescheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
