package io.github.notkanaa.equipe.contract

import io.github.notkanaa.equipe.core.AppServices

/**
 * Placeholder of the backend-agnostic contract scenarios (Android port of TeamTasksContract): the same scenarios
 * run against the in-memory backend (:mocks) and against a real Supabase stack (:supabase), so both implementations
 * of [AppServices] behave identically.
 */
object TeamTasksContract {
    const val PLACEHOLDER: String = "contract"
}
